<%= @list.name %><br>
<%= @list.public %><br>

<h2>Tasks</h2>
<% @list.tasks.each do |task| %>
  <%= check_box "task", "done", { checked: task.done, disabled: true } %>
  <%= task.text %><br>
<% end %>
